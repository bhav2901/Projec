1) ---src/RoutesConfig.js

import React from "react";
import { Routes, Route } from "react-router-dom";

import FilesPanel from "./components/Filespanel/FilesPanel";
import Downloads from "./components/Downloads";
import Favorites from "./components/Favorites";

// Later you can add:
// import Settings from "./components/Settings";
// import Profile from "./components/Profile";

export default function RoutesConfig() {
  return (
    <Routes>
      <Route path="/" element={<FilesPanel />} />
      <Route path="/downloads" element={<Downloads />} />
      <Route path="/favorites" element={<Favorites />} />
      {/* Future pages go here */}
      {/* <Route path="/settings" element={<Settings />} /> */}
    </Routes>
  );
}




2)src/App.js

import React from "react";
import Sidebar from "./components/Sidebars/Sidebar";
import Navbar from "./components/Navbar";
import TopBlue from "./components/TopBlue";
import RoutesConfig from "./RoutesConfig";

export default function App() {
  return (
    <div className="bg-light">
      {/* Page Title */}
      <header className="py-2 text-center user-title">
        <h1 className="mb-0">User Dashboard</h1>
      </header>

      <div className="container-fluid px-4">
        <div className="row gx-3">
          {/* LEFT Sidebar */}
          <aside className="col-md-3 col-lg-2 pe-0">
            <Sidebar />
          </aside>

          {/* RIGHT Main */}
          <main className="col-md-9 col-lg-10">
            <Navbar />
            <TopBlue />
            <div className="container mt-3">
              <RoutesConfig /> {/* ✅ Centralized routes */}
            </div>
          </main>
        </div>
      </div>
    </div>
  );
}



3) src/index.js

import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";

import "bootstrap/dist/css/bootstrap.min.css";
import "bootstrap-icons/font/bootstrap-icons.css";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <BrowserRouter>
    <App />
  </BrowserRouter>
);


4) src/components/Sidebars/Sidebar.js

import React from "react";
import { Link } from "react-router-dom";

export default function Sidebar() {
  return (
    <div className="sidebar border rounded-start shadow-sm bg-white p-3">
      <h5 className="mb-3">SCB</h5>

      <div className="list-group">
        <Link to="/" className="list-group-item list-group-item-action">
          Dashboard
        </Link>
        <Link to="/downloads" className="list-group-item list-group-item-action">
          Downloads
        </Link>
        <Link to="/favorites" className="list-group-item list-group-item-action">
          Favorites
        </Link>
        {/* Example future merge */}
        {/* <Link to="/settings" className="list-group-item list-group-item-action">
          Settings
        </Link> */}
      </div>
    </div>
  );
}
